class ResultSummary {
  int correctAnswers = 0;
  int incorrectAnswers = 0;
  int timeSpent = 0;
  bool won = false;

  ResultSummary(
      this.correctAnswers, this.incorrectAnswers, this.timeSpent, this.won);
}
